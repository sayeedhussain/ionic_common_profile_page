# Common Web Components Repository

Contains web components that can be reused across projects.

# Includes

Web Component for Profile Page

# Dependencies

- node & npm
- ionic `npm install -g ionic`
- stencil

# Run

From project root, use `npm start`
